<% @items.each do |item| %>
     
        
          <div><%= item.id %></div>
      <div><%=link_to item.name, admin_item_path(item), class: "text-dark" %></div>
          <div><%= item.price %></div>
<% end %>
<h1>aaa</h1>