

<div class="container-fluid"><br>
  <div class="row">
    <div class="col-1"></div>
    <div class="col-2">

    </div>
    <div class="col-8">
      商品一覧 全<%= @items_count.count %>件<br>
      <div class="row">
  <% @items.each do |item| %>
  <%=image_tag item.item_image, size:'100x100' %>
    <div class="m-2">
    <%= link_to item_path(item.id) do %>
    <% end %><br>
    <%= link_to item.name, item_path(item.id), class: "font-weight-bold" %><br>
      <%= item.with_tax_price %>（税込）
    </div>
  <% end %>
</div>
    </div>
    <div class="col-1"></div>
  </div>
</div>